# Agentic HR

LangGraph-based multi-agent recruitment evaluation workflow.

## Quick Start

```powershell
python -m pip install -r requirements.txt
python main.py
```

The current version is a walking skeleton with mock nodes. It runs the workflow end to end without LLMs, PDF parsing, databases, or ML models.
